export type PostInputModel = {
  title: string;
  shortDescription: string;
  content: string;
  blogId: string;
};

export type PostInBlogInputModel = {
  title: string;
  shortDescription: string;
  content: string;
};
